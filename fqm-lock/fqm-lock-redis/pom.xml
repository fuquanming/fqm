<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.fuquanming</groupId>
        <artifactId>fqm-lock</artifactId>
        <version>1.0.2</version>
    </parent>
    <artifactId>fqm-lock-redis</artifactId>
    <description>Redis distributed locks are implemented based on RedisTemplate (blocking locks, trying to acquire locks once, trying to acquire locks, and unlocking)</description>
    
    <properties>
        <fqm-common-redis.version>1.0.4</fqm-common-redis.version>
    </properties>
    
    <dependencies>
        <!-- redis监听事件 -->
        <dependency>
            <groupId>io.github.fuquanming</groupId>
            <artifactId>fqm-common-redis</artifactId>
            <version>${fqm-common-redis.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.fuquanming</groupId>
            <artifactId>fqm-lock-core</artifactId>
            <version>${parent.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>